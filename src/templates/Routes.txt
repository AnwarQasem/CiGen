

// {model} Routes

$routes->get('{table}/(:num)',        '{model}::index/$1');
$routes->get('{table}/show/(:num)',   '{model}::show/$1');
$routes->get('{table}/fields',        '{model}::fields');

$routes->post('{table}',              '{model}::add');
$routes->post('{table}/(:num)',       '{model}::update/$1');

$routes->delete('{table}/(:num)',     '{model}::delete/$1');
